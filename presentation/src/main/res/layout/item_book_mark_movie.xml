<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="movie"
            type="com.navermovie.entity.Movie" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_book_mark_poster"
            bindPoster="@{movie.poster}"
            android:layout_width="140dp"
            android:layout_height="180dp"
            android:contentDescription="@null"
            android:scaleType="fitXY"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@tools:sample/avatars" />

        <TextView
            android:id="@+id/tv_book_mark_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:autoSizeTextType="uniform"
            android:background="@drawable/gradient"
            android:gravity="center"
            android:maxLines="2"
            android:paddingBottom="10dp"
            android:text="@{movie.title}"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/iv_book_mark_poster"
            app:layout_constraintEnd_toEndOf="@id/iv_book_mark_poster"
            app:layout_constraintStart_toStartOf="@id/iv_book_mark_poster"
            tools:text="블랙아담" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>